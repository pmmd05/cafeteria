# Puerto donde correrá Express
PORT=3000
# Cadena de conexión para pg. Host apunta a tu máquina (localhost)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cafeteria
# Modo ejecución. En prod se oculta el stack trace en el error handler
NODE_ENV=development

